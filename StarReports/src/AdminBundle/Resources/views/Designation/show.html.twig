{% extends 'base.html.twig' %}

{% block body %}
    <h1>Designation</h1>

    <table>
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ designation.id }}</td>
            </tr>
            <tr>
                <th>Named</th>
                <td>{{ designation.named }}</td>
            </tr>
            <tr>
                <th>Numberemployee</th>
                <td>{{ designation.numberEmployee }}</td>
            </tr>
        </tbody>
    </table>

    <ul>
        <li>
            <a href="{{ path('designation_index') }}">Back to the list</a>
        </li>
        <li>
            <a href="{{ path('designation_edit', { 'id': designation.id }) }}">Edit</a>
        </li>
        <li>
            {{ form_start(delete_form) }}
                <input type="submit" value="Delete">
            {{ form_end(delete_form) }}
        </li>
    </ul>
{% endblock %}
